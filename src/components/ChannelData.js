

const ChannelData = ({description}) => {
const {title}=description;

   
    
  return (
    <div>
        <h1>{title}</h1>
        {/* <h1>{channelTitle}</h1> */}
    </div>
  )
}

export default ChannelData